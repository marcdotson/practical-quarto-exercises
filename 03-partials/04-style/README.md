# Your turn 4: style what you built

Don't edit `title-block.html`.

1. Ask Posit Assistant to write `custom.scss`. Open with the prompt below, then
   add how you want it to look.

   > I'm in `@03-partials/04-style`. Write SCSS in `custom.scss`, a Quarto theme
   > file. My Quarto title block partial adds three classes: `report-masthead`,
   > `report-period`, and `report-status`. Use our brand variables —
   > `$primary`, `$secondary`, `$brand-forest`, `$text-muted` — and never a
   > hard-coded color.

2. Render.

3. Change `primary: forest` to `primary: slate` in `_brand/_brand.yml`. Render
   again.

A finished answer is in `03-partials/05-final`.
