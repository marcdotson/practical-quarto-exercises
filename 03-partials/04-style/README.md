# Your turn 4: style what you built

Don't edit `title-block.html`.

1. Ask Posit Assistant to write `custom.scss`. Open with the prompt below, then
   add how you want it to look.

   ```
   I'm in @03-partials/04-style. Write SCSS in custom.scss, a Quarto theme file. My Quarto title block partial adds three classes: report-masthead, report-period, and report-status. Use our brand variables ($primary, $secondary, $brand-forest, $text-muted) and never a hard-coded color.
   ```

2. Render. Not what you asked for? Say what is wrong and go again. Iterating is
   the workflow, not a sign that it failed.

3. Read `custom.scss`. Change one of the brand colors it used in
   `_brand/_brand.yml`, and render.

   Did everything you expected to change, change?

A finished answer is in `03-partials/05-final`.
